{
  "name": "backy",
  "version": "1.1.43",
  "description": "基于 Tauri 的增量备份桌面工具，支持定时/即时/任务调度。",
  "author": "local",
  "type": "module",
  "scripts": {
    "start": "tauri dev",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "check": "node --check app/renderer/app.js && node --check app/renderer/tauri-bridge.js && cargo check --manifest-path src-tauri/Cargo.toml",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "pack": "node tools/tauri.cjs build --no-bundle",
    "dist": "npm run clean && node tools/tauri.cjs build",
    "tauri": "node tools/tauri.cjs",
    "clean": "node tools/tauri.cjs clean"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
